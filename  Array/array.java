public class Main {

    public static void main (Strings[] args) {
        //datatype[] var_name = new datatype[size];
        int[] roll = new int[20];

        // int[] roll; declaration in the stack : compile time 
        // roll = new int[5] initialization in heap memory : runtime dynamic memory alloc.

        system.out.println(roll[1])

    }
}